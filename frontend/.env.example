# Backend API base. Leave unset for local/docker (nginx proxies /api/*).
# Set to a tunnel origin for Vercel preview testing.
# NEXT_PUBLIC_API_BASE=

# Enable the cockpit/app UI. Unset = dark (production default).
# NEXT_PUBLIC_ENABLE_COCKPIT=true
